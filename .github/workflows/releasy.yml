name: C++ CI and Release

on:
  workflow_dispatch: 

jobs:
  build_and_package:
    name: 🏗️ Build and Package
    runs-on: ubuntu-latest
    outputs:
      package_path: ${{ steps.package.outputs.package_path }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download GoogleTest manually (для вашего CMake add_subdirectory)
        run: |
          mkdir -p external/googletest
          cd external/googletest
          git init
          git remote add origin https://github.com/google/googletest.git
          git fetch --depth=1 origin main
          git checkout main

      - name: Install dependencies for build and CPack
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential debhelper dpkg-dev

      - name: Configure with CMake
        run: |
          mkdir -p build
          cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release

      - name: Build and Package .deb file (ИСПОЛЬЗУЕМ НОВЫЙ СПОСОБ ВЫВОДА)
        id: package
        run: |
          cd build
          cmake --build . --config Release --target package
          # Записываем путь в файл $GITHUB_OUTPUT (современный метод)
          echo "package_path=$(pwd)/tests-0.0.1-Linux.deb" >> $GITHUB_OUTPUT


  create_release:
    name: 🚀 Create Release and Upload Asset
    needs: build_and_package
    runs-on: ubuntu-latest

    steps:
      - name: Create Release (ИСПОЛЬЗУЕМ ВЕРСИЮ v3)
        id: create_release
        uses: actions/create-release@v3 # СТАБИЛЬНАЯ ВЕРСИЯ v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: 'v0.0.1'
          release_name: Release v0.0.1
          draft: false
          prerelease: false

      - name: Upload Release Asset (.deb file) (ИСПОЛЬЗУЕМ ВЕРСИЮ v3)
        id: upload-release-asset-deb
        uses: actions/upload-release-asset@v3 # СТАБИЛЬНАЯ ВЕРСИЯ v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          
          # Берем путь из задания сборки
          asset_path: ${{ needs.build_and_package.outputs.package_path }}
          
          asset_name: tests-0.0.1-Linux.deb 
          asset_content_type: application/vnd.debian.binary-package
